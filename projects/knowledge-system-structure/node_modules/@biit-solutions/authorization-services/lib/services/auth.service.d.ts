import { HttpClient, HttpResponse } from "@angular/common/http";
import { Observable } from "rxjs";
import { User } from "../models/user";
import { CreateUserRequest } from "../models/create-user-request";
import { LoginRequest } from "../models/login-request";
import { PasswordRequest } from "../models/password-request";
import { TokenRenewListener } from "./token-renew";
import { AuthCalls } from "./auth-calls";
import { RootPath } from "../models/root-path";
export declare class AuthService implements AuthCalls {
    private rootPath;
    private httpClient;
    static readonly ROOT_PATH: string;
    private static readonly MIN_WAIT_TIME;
    private static readonly TOLERANCE;
    private timeoutId;
    constructor(rootPath: RootPath, httpClient: HttpClient);
    getAll(): Observable<User[]>;
    add(user: User): Observable<User>;
    update(request: CreateUserRequest): Observable<User>;
    login(request: LoginRequest): Observable<HttpResponse<User>>;
    changePassword(request: PasswordRequest): Observable<void>;
    getRoles(): Observable<string[]>;
    deleteByUserName(username: string): Observable<void>;
    cancelAutoRenew(): void;
    loginByToken(token: string): Observable<HttpResponse<User>>;
    autoRenewToken(token: string, expiration: number, tokenRenewListener: TokenRenewListener, tolerance?: number): void;
    private setTimeoutRenew;
    renew(token?: string): Observable<HttpResponse<User>>;
}
