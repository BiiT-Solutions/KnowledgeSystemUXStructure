import { AuthService } from "./auth.service";
import { TokenRenewListener } from "./token-renew";
import { User } from "../models/user";
export declare class SessionService implements TokenRenewListener {
    private authService;
    private context;
    private loggedIn;
    private user;
    private store;
    constructor(authService: AuthService, context?: string);
    onTokenReceived(token: string, expiration: number, user: User): void;
    onException(error: any): void;
    private setAutoRenew;
    clearToken(): void;
    setToken(token: string, expires: number, enableStore?: boolean, autoRenew?: boolean): void;
    getToken(): string;
    isTokenExpired(): boolean;
    getExpirationDate(): Date;
    get isLoggedIn(): boolean;
    setUser(user: User, enableStore?: boolean): void;
    getUser(): User;
}
